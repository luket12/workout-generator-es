// The workout outputter can print output
test('The output is printed to console', () => {

});

// The workout outputter prints the expected number of lines for workout sets
test('The output prints the expected number of sets', () => {

});

// The workout outputter outputs the expected workout start and end times
test('The output prints the expected workout start and end time', () => {

});

// The workout outputter outputs the expected set start and end time
test('The output prints the expected start and end set time', () => {

});